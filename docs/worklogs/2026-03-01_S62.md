# Session 62 — 2026-03-01

## Context
- **Phase**: Phase 3 — Partial Observability & Self-Play
- **Branch**: main

## Objectives
- [x] Add `greedy_full_obs` flag to `SingleAgentPEWrapper` so greedy pursuer gets unmasked observations
- [x] Add `--greedy_full_obs` CLI arg to `train_evader_vs_greedy.py`
- [x] Add tests for greedy_full_obs behavior
- [ ] Launch S1v5c on niro-2

## Work Done

### greedy_full_obs Implementation
- **Problem**: S1v5b (combined masking, 96% peak) escape rate is inflated because the greedy pursuer is also blinded by symmetric partial obs. When evader is out of sensing range (>3.0m), the greedy pursuer's obs has zeros for opponent position, causing it to steer toward center instead of chasing.
- **Fix**: Added `greedy_full_obs` param to `SingleAgentPEWrapper.__init__()`. When enabled and `role == "evader"`, the wrapper builds an unmasked observation for the opponent (pursuer) using `obs_builder.build(..., los_blocked=False)` before passing it to `opponent_policy.predict()`.

### CLI Integration
- Added `--greedy_full_obs` (store_true) arg to `train_evader_vs_greedy.py`
- Plumbed through `make_env()` and `evaluate()` functions
- Updated banner to show `Greedy obs: FULL (unmasked)` vs `Greedy obs: MASKED (same as evader)`
- Updated baseline loop to build unmasked obs for greedy pursuer when flag is set

### Tests
- `test_greedy_full_obs_sees_evader`: Verifies that with `greedy_full_obs=True`, the full obs built for the pursuer has non-zero opponent features even when evader is out of sensing range
- `test_greedy_default_masked_when_out_of_range`: Confirms default behavior — pursuer obs is masked when out of range without the flag

## Files Changed
| File | Action | Description |
|------|--------|-------------|
| `envs/wrappers.py` | Modified | Added `greedy_full_obs` param and full-obs override in `step()` |
| `scripts/train_evader_vs_greedy.py` | Modified | Added `--greedy_full_obs` CLI arg, plumbed through make_env/evaluate, updated banner and baseline |
| `tests/test_env.py` | Modified | Added 2 tests to `TestRadiusSensing` |
| `docs/worklogs/2026-03-01_S62.md` | Created | This worklog |

## Decisions Made
| Decision | Rationale |
|----------|-----------|
| Override obs in wrapper, not env | Keeps env's `_get_obs()` pure — the full-obs override is specific to the diagnostic wrapper use case |
| Reuse `obs_builder.build()` with `los_blocked=False` | No new code needed for obs construction, just bypasses the masking |

## Issues & Blockers
| Issue | Resolution |
|-------|------------|
| None | — |

## Next Steps
- [ ] Push to GitHub, pull on niro-2, launch S1v5c
- [ ] Monitor S1v5c escape rate — should be significantly lower than S1v5b's 96%
- [ ] Compare S1v5b vs S1v5c to quantify how much of the escape rate was from pursuer blindness

## Metrics
- Files changed: 3
- Tests added: 2
- Total tests passing: 738 (env), 733 (full suite, 68 pre-existing CBF failures)
