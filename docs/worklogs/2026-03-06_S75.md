# Session 75 — 2026-03-06/07

## Context
- **Phase**: Phase 3 — Partial Observability & Self-Play
- **Branch**: main

## Objectives
- [x] Implement masking curriculum (PO-GRL style p_full_obs annealing)
- [x] Push code and launch masking curriculum runs on niro-2
- [x] Launch additional experiment variants (slow anneal, low search reward)
- [x] Move results to /mnt/data2 for disk space

## Work Done

### Masking Curriculum Implementation
Implemented the PO-GRL-style masking curriculum across three layers:

1. **Wrapper layer** (`envs/wrappers.py`):
   - Added `p_full_obs` parameter to `SingleAgentPEWrapper.__init__`
   - Added per-timestep observation override in `step()`: with probability `p_full_obs`, rebuild the observation with `los_blocked=False` (full visibility)
   - Uses `np.random.default_rng()` for independent random state

2. **Environment creation** (`training/amsdrl.py`):
   - Added `p_full_obs` parameter to `_make_partial_obs_env()` and `_make_vec_env()`
   - Passed through to `SingleAgentPEWrapper` on construction

3. **Training loop** (`training/amsdrl.py`):
   - Added `masking_curriculum`, `p_full_anneal_steps`, `p_full_residual` params to `AMSDRLSelfPlay.__init__`
   - In `_run_micro_phases()`: linear annealing of `p_full_obs` from 1.0 → `p_full_residual` over `p_full_anneal_steps`
   - Updates `p_full_obs` on all pursuer sub-envs via wrapper chain traversal before each micro-phase
   - Logs `p_full_obs` in eval entries and status line

4. **CLI** (`scripts/train_amsdrl.py`):
   - Added `--masking_curriculum`, `--p_full_anneal_steps`, `--p_full_residual` flags

5. **Tests** (`tests/test_env.py`):
   - `test_masking_curriculum_p_full_obs`: Verifies p_full_obs=1.0 always gives full obs
   - `test_masking_curriculum_p_full_obs_zero`: Verifies p_full_obs=0.0 preserves masking

All 679 non-BarrierNet tests pass.

### Training Runs Launched

**SP12_mc (4 seeds)** — Masking curriculum baseline:
- Config: SAC, freeze_evader, combined masking (3m+LOS), 0-3 obstacles, p_full 1.0→0.0 over 5M steps
- Seeds 42-45, 30M max steps each
- Results at ~8.3M steps before restart (see below):
  - s42: best 0.450, avg5 0.264 (weak seed)
  - s43: best 0.500, avg5 0.418 (most consistent)
  - s44: best 0.540, avg5 0.372 (rising star)
  - s45: best 0.530, avg5 0.332 (late bloomer)
- Annealing completed at 5M steps; post-annealing performance held at 30-50%
- **+10-15pp improvement over SP11i baseline** (avg ~30% at same step count)

**SP12b_slow (2 seeds)** — Slower anneal variant:
- Same as SP12 but `--p_full_anneal_steps 10000000` (10M instead of 5M)
- Seeds 42, 43

**SP12c_search (2 seeds)** — Low search reward variant:
- Same as SP12 plus `--w_search 0.0001` (30x lower than the harmful 0.003)
- Seeds 42, 43

### Disk Management
- Killed 3 plateau'd SP11 runs (SP11f2, SP11i_s43, SP11i_s44)
- Cleaned 34GB of old dead runs from root disk
- **Moved results/ to /mnt/data2** (458GB disk, 423GB free) via symlink:
  - `results/ → /mnt/data2/safe-rl-pe-results`
  - Root disk went from 89% → 73% used
- **Mistake**: Had to kill all 8 runs to move results directory. AMSDRLSelfPlay has no resume mechanism, so SP12_mc runs lost ~8.3M steps each (~5.5 hours per seed). Runs restarted from scratch. Best model checkpoints preserved on disk.

### Visualization
- Generated trajectory PNGs and 3x3 grid GIFs for all 4 SP12_mc seeds
- Fixed visualization script default: was using `--max_steps 600` instead of 2000
- With correct 2000-step episodes, capture rates were much higher (67-78% vs 22-44%)

## Files Changed
| File | Action | Description |
|------|--------|-------------|
| `envs/wrappers.py` | Modified | Added `p_full_obs` param and observation override logic |
| `training/amsdrl.py` | Modified | Added masking curriculum params, annealing in micro-phase loop |
| `scripts/train_amsdrl.py` | Modified | Added `--masking_curriculum` CLI flags |
| `tests/test_env.py` | Modified | Added 2 masking curriculum tests |
| `docs/worklogs/2026-03-06_S75.md` | Created | This worklog |
| `docs/workflow_tracker.md` | Modified | Added S75 entry |

## Decisions Made
| Decision | Rationale |
|----------|-----------|
| Only apply masking curriculum to pursuer envs | Pursuer is the one struggling with search under partial obs |
| Linear anneal formula: `p = (1-progress)(1-residual) + residual` | Simple, smooth transition |
| Per-timestep random override (not per-episode) | Matches PO-GRL approach |
| Let restarted SP12_mc runs train from scratch | Warm-seeding SAC without replay buffer causes catastrophic forgetting (SP11e2 lesson) |
| Move results to /mnt/data2 via symlink | Root disk at 89%, /mnt/data2 had 433GB free |

## Issues & Blockers
| Issue | Resolution |
|-------|------------|
| SP12_mc runs lost 8.3M steps on restart | No resume mechanism. Accepted the loss; runs restarting from scratch |
| Visualization used max_steps=600 default | Regenerated with --max_steps 2000 to match training |
| Disk at 89% on root | Moved results to /mnt/data2, cleaned old runs |

## Next Steps
- [ ] Monitor SP12_mc runs through annealing phase (~3 hours to 5M steps)
- [ ] Check SP12b (slow anneal) and SP12c (search reward) first evals
- [ ] Add resume mechanism to train_amsdrl.py (save/load replay buffer + training state)
- [ ] Generate new GIFs once SP12_mc runs reach 8M+ steps again

## niro-2 Disk Layout (for reference)
| Disk | Mount | Size | Free | Contents |
|------|-------|------|------|----------|
| nvme0n1p2 | `/` | 292G | 75G | OS, project code |
| nvme1n1p1 | `/mnt/data1` | 458G | 318G | basedir, old runs |
| nvme1n1p2 | `/mnt/data2` | 458G | 423G | **Our results (symlinked)** |
| nvme1n1p3 | `/mnt/isaac` | 916G | 853G | Isaac Sim |
| sda (HDD) | unmounted | 1.8T | ? | 3 partitions |
